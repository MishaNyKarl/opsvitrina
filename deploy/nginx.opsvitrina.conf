server {
    listen 169.239.181.248:80 default_server;
    listen 80;
    listen [::]:80;
    server_name stagingopsvitrinaru.lol;

    client_max_body_size 50m;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type "text/plain";
    }

    location /static/ {
        alias /opt/opsvitrina/staging/staticfiles/;
        access_log off;
        expires 30d;
    }

    location /media/ {
        alias /opt/opsvitrina/staging/media/;
        access_log off;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:8010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name read.lifestoruhabstt.info;

    client_max_body_size 50m;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type "text/plain";
    }

    location /static/ {
        alias /opt/opsvitrina/production/staticfiles/;
        access_log off;
        expires 30d;
    }

    location /media/ {
        alias /opt/opsvitrina/production/media/;
        access_log off;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:8020;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
